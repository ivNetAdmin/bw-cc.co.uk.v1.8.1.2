@{    
    Script.Require("CKEditor");
    Script.Require("AngularJS-Resource");
    Script.Require("UI.Bootstrap");
    Script.Require("Admin.Report.New");

    Style.Require("Admin.Report.New");
    Style.Require("trNgGrid");
}

<div data-ng-app="Admin.Report.New.App" class="col-md-12">
    <div data-ng-controller="AdminReportController">
        <div class="col-md-9 fixture-list">
            @Html.Partial("Admin/Fixture/_Shared/_List")
        </div>

        <div class="col-md-9 selected-fixture">            
            @Html.Partial("Admin/Fixture/Report/_MatchReport")
        </div>

        <div class="col-md-3 selected-fixture">
            <div class="col-md-12">
                {{selectedFixture.Date | date:"dd/MM/yyyy"}}<br />{{selectedFixture.Team}} v {{selectedFixture.Opponent}}<br />{{selectedFixture.HomeAway}}, {{selectedFixture.FixtureType}}
            </div>
            <div class="col-md-12">
                Result:
                <select style="width:115px" ng-model="fixtureResult">
                    <option ng-selected="{{opt.Id == fixtureResult}}"
                            ng-repeat="opt in results"
                            value="{{opt.Id}}">
                        {{opt.Name}}
                    </option>
                </select>
            </div>
            <div class="col-md-12">
                Score:
                <textarea ng-model="score"></textarea>
            </div>
        </div>
    </div>
</div>
